using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RopeBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /* Object stops at the rigidbody it collides with, render a linerenderer between it and the players hand (?).
          The next shot from the player connects the second point for the linerenderer and spring joint*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
